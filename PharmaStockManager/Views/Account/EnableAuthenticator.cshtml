@{
    ViewData["Title"] = "Enable Two-Factor Authentication";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Enable Two-Factor Authentication</h2>
<p>To enable 2FA, scan the QR code below using an authenticator app (e.g., Google Authenticator).</p>

<img src="@Model.QrCodeUrl" alt="QR Code for 2FA" />

<p>Or, you can manually enter the following key into your authenticator app:</p>
<code>@ViewBag.AuthenticatorKey</code>

<form asp-action="VerifyAuthenticatorCode" method="post">
    <label for="code">Enter the verification code generated by your app:</label>
    <input type="text" id="code" name="code" required />
    <button type="submit">Enable 2FA</button>
</form>
